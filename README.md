# AI Git Assistant

A minimal VS Code extension foundation for Git assistance features.

## Development

```bash
npm install
npm run compile
```

Press `F5` in VS Code to launch the Extension Development Host, then run **AI Git Assistant: Hello World** from the Command Palette.
